.venv/
.venv-*/
.env
pages/
out/
__pycache__/
*.pyc
# Per-book active config (contains personal paths). Users start from
# book.example.yaml — see README.
book.yaml
books/
# Per-machine Claude Code session settings
.claude/settings.local.json

# Mac .app build artifacts (the build/ scripts themselves are tracked).
# Per-arch PyInstaller workdirs:
build/_pyinstaller*/
# Cached python-build-standalone downloads + extracts:
build/_pbs/
# Generated from src/gui/resources/book.svg:
build/book.icns
dist/
