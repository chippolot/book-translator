anthropic>=0.40
google-genai>=0.3
openai>=1.40
python-dotenv>=1.0
pyyaml>=6.0
PySide6>=6.6
keyring>=24
ruamel.yaml>=0.18
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
pypdfium2>=4.30
Pillow>=10.0

# Mac .app build tooling (not needed to run the app, only to build it).
pyinstaller>=6.20
dmgbuild>=1.6
